# Updating relative path for code files

Relative images and links in the imported files will have their paths rewritten
to be relative to the original document rather than the imported file.

Source files:

main.md:

::include-code{file=fixtures/main.md language=markdown}

included.md:

::include-code{file=fixtures/subfolder1/included.md language=markdown}

Remark output:

::include-code{file=fixtures/output.md language=markdown}
